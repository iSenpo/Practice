#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ln '\n'
#define fo(i, n) for(int i = 0 ; i < n ; i++)
int MOD = 1e9 + 7;
const int N  = 1e5 + 500;
int arr[N];

void solve()
{

}
int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    /*------------------------------------*/
    string s;
    cin >> s;
    int size = s.size();
    s = s + "  ";
    int left = 0 , right = 0;
    int counter = 0;
    for(int i = 0 ; i < size-1 ; i++){
        if(s[i] == 'A'){

        }
    }
    cout << counter << ln;
    return 0;
}