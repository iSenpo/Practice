#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ln '\n'
#define fo(i, n) for(int i = 0 ; i < n ; i++)
int MOD = 1e9 + 7;
const int N  = 1e9;
int arr[N];

void solve()
{

}
int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    /*------------------------------------*/
    int n , x;
    cin >> n >> x;
    int v[n] , w[n];
    for(int i = 0 ; i < n ; i++){
        cin >> v[i] >> w[i] ;
    }
    int dp[n];
    memset(dp , N , n * sizeof(int));
    for(int i = 0 ; i < n ; i++){
        for(int j = 0 ; j < x ; j++){
            
        }
    }
    return 0;
}